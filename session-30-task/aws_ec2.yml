plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

keyed_groups:
  - key: tags
    prefix: tag

# hostnames:
#   - ip_address
#   - public_dns_name
#   - private_dns_name

# compose:
#   ansible_host: "{{ public_ip_address }}"
#   ansible_user: "{{ 'ubuntu' if (tags.Name is defined and 'ubuntu' in tags.Name | lower) else 'ec2-user' }}"
#   ansible_ssh_private_key_file: "~/.ssh/ansible_key.pem"
